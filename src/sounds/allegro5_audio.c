#include "sounds/allegro5_audio.h"

void SoundsInit(int maxSounds, int maxMusic, unsigned int freq, int channels, int chunksize)
{
//    uint32_t version;
//    //size_t depth;
//
//    LogWrite("Initializing Sound Manager", 0, MT_INFO, NULL);
//	LogWrite("Installing audio subsystem", 1, MT_INFO, NULL);
//	if (al_install_audio())
//	{
//		LogWrite("Audio subsystem installed", 1, MT_INFO, NULL);
//	}
//	else
//	{
//		LogWrite("Can not install audio subsystem", 1, MT_INFO, NULL);
//	}
//
//	version = al_get_allegro_audio_version();
//    LogWrite("Checking Allegro Audio Addon compile-time version", 1, MT_INFO, NULL);
//    LogWrite2("Major:", 2, MT_INFO, version >> 24);
//    LogWrite2("Minor:", 2, MT_INFO, (version >> 16) & 255);
//    LogWrite2("Revision:", 2, MT_INFO, (version >> 8) & 255);
//    LogWrite2("Release:", 2, MT_INFO, version & 255);
//
//
//
//    Sound = malloc(sizeof(Mix_Chunk*) * maxSounds);
//    if (Sound == NULL)
//        ErrorGive("Can not allocate memory for Sounds", 1);
//    Music = malloc(sizeof(Mix_Music*) * maxMusic);
//    if (Music == NULL)
//        ErrorGive("Can not allocate memory for Music", 1);
//
//    LogWrite("Sound Manager initialized", 0, MT_INFO, NULL);
}


